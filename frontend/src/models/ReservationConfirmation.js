class ReservationConfirmation{
    constructor(reference, reservationId, price) {
        this.reference = reference;
        this.price = price;
        this.reservationId = reservationId;
    }
}

export default ReservationConfirmation;